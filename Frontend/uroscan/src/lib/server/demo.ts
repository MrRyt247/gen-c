import type { SessionUser } from '$lib/types';

/** The fictional user signed in while running in demo mode. */
export const DEMO_USER: SessionUser = {
	id: 'demo-user-0001',
	email: 'demo@uroscan.app',
	name: 'Ama Mensah'
};
